@Library('quickstart') _
  
qs {
    plugin = 'loopback'
    nodejsVersion = 'nodejs10'
    dockerRegistry = 'cnx-tools-docker.artifactory.cwp.pnp-hcl.com'
    deploy = false
    utilizeClair = false
}
